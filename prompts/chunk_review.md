请审查以下单个 diff chunk，并只返回 JSON，不要输出 markdown、解释或额外文本。

这一步虽然是分片审查，但仍然要遵循与整 MR 审查一致的“重度规范驱动”原则：只保留真正值得进入最终汇总的问题，所有自然语言输出必须为简体中文。

审查维度：
1. 安全与权限边界：鉴权、越权、输入校验、敏感信息、接口安全、TLS/证书校验、危险调试开关。
2. 正确性与边界条件：条件分支、状态变更、空值、越界、异常路径、返回值语义。
3. 事务/并发/一致性：事务边界、锁、幂等、竞态、缓存一致性、消息重复或丢失风险。
4. 稳定性与资源管理：异常处理、超时、重试、连接/句柄/线程/协程释放、死循环、崩溃风险。
5. 性能与可维护性：热点路径性能退化、重复 IO、复杂度失控、日志监控缺失、测试缺口、职责混乱。

高优先级判定规则：
- `severity = high`：表示“必须修改”。用于安全漏洞、核心逻辑错误、事务/并发一致性问题、高概率运行时故障、严重配置误用。
- `severity = medium`：表示“建议修改”。用于明确的工程风险、维护成本或排障风险。
- `severity = low`：表示“可选优化”。仅在建议收益明确时输出；没有高价值建议时可为空。
- 证据不足时降低 `confidence`，不要盲目抬高 `severity`。

输出 JSON 结构：
{
  "summary": "一句话中文结论，直接说明该分片是否发现值得进入最终报告的问题",
  "issues": [
    {
      "severity": "high|medium|low",
      "confidence": "high|medium|low",
      "title": "必须修改/建议修改/可选优化｜分类｜问题",
      "reason": "说明问题触发点、风险影响以及为什么值得进入最终汇总",
      "suggestion": "给出可执行的修复建议"
    }
  ],
  "uncertainty_notes": ["仅在证据不足但值得提醒时填写中文说明，否则返回空数组"]
}

补充要求：
1. 只记录真正值得进入最终汇总的问题，不要输出无意义建议。
2. 默认忽略纯格式、纯命名偏好、缺乏证据的猜测。
3. 如果未发现问题，返回合法 JSON，`issues` 为空数组。
4. 不要输出 markdown 代码块，不要输出额外解释。
